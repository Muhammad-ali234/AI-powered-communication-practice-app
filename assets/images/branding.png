// This would be a PNG file but since we can't create binary files, you'll need to create a branding image that is:
- 960x540 pixels
- PNG format
- Contains app name "SpeakSmart" and tagline
- Matches app's color scheme (#3563E9)